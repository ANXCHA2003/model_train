# 🥩 Meat Model Trainer - ระบบเทรนโมเดล

## 📋 คำอธิบาย

โปรแกรม GUI บน Windows (สร้างด้วย CustomTkinter) สำหรับ:

- 📦 การเพิ่มข้อมูล (ภาพหรือไฟล์ข้อมูล)
- 🧹 การตรวจสอบและทำความสะอาดข้อมูล
- 🤖 การเทรนโมเดล Machine Learning / Deep Learning
- 📤 การบันทึกโมเดลออกเป็นไฟล์ .h5 และ .tflite

---

## 📁 โครงสร้างไฟล์

```bash
meat_model_trainer/
│
├── main.py                     # โปรแกรมหลัก (GUI)
├── requirements.txt             # รายชื่อ libraries ที่ต้องใช้
├── README_INSTALL.md            # คู่มือติดตั้งและใช้งาน
│
├── data/                        # โฟลเดอร์เก็บข้อมูล
│   ├── images/                  # รูปภาพที่อัปโหลด
│   └── uploaded_data.csv        # ไฟล์ข้อมูล
│
├── models/                      # เก็บไฟล์โมเดลที่เทรนเสร็จ (.h5, .tflite)
│
└── modules/                     # แยกฟังก์ชันเป็นหมวด
    ├── data_loader.py           # โหลด / อัปโหลด / จัดเก็บไฟล์
    ├── data_validator.py        # ตรวจสอบคุณภาพข้อมูล
    ├── model_trainer.py         # เทรนโมเดลและบันทึก
    ├── product_manager.py       # จัดการข้อมูลสินค้า
    └── ui_components.py         # องค์ประกอบ GUI
```

---

## 🛠️ การติดตั้ง

### 1. ติดตั้ง Python 3.8+

ดาวน์โหลดจาก [python.org](https://www.python.org/downloads/)

### 2. สร้าง Virtual Environment (เสริม)

```bash
python -m venv venv
venv\Scripts\activate
```

### 3. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

> **⚠️ หมายเหตุ**: ติดตั้ง TensorFlow อาจใช้เวลาหลายนาที

---

## 🚀 วิธีใช้งาน

### เริ่มต้นโปรแกรม

```bash
python main.py
```

---

## 🖥️ หน้าหลักของโปรแกรม (3 แท็บ)

### 1️⃣ แท็บ "📦 เพิ่มข้อมูล"

**อัปโหลดไฟล์ภาพ:**

- คลิก "📁 เลือกไฟล์" เลือกไฟล์ภาพ (.jpg, .png, .bmp, .gif)
- คลิก "💾 บันทึกไฟล์ภาพ" เพื่อบันทึก
- ไฟล์จะถูกบันทึกใน `data/images/`

**อัปโหลดไฟล์ข้อมูล:**

- คลิก "📁 เลือกไฟล์" เลือกไฟล์ (.csv, .xlsx, .json)
- คลิก "💾 บันทึกไฟล์ข้อมูล" เพื่อบันทึก
- ไฟล์จะถูกแปลงเป็น CSV บันทึกใน `data/uploaded_data.csv`

**ดูสถานะข้อมูล:**

- คลิก "🔄 รีเฟรช" เพื่อดูจำนวนไฟล์และแถว

---

### 2️⃣ แท็บ "🧹 ตรวจสอบข้อมูล"

**ตรวจสอบข้อมูล:**

- คลิก "📋 ตรวจสอบข้อมูล" เพื่อดูรายงาน
- จะแสดง:
  - จำนวนแถวและคอลัมน์
  - ค่าว่าง (Missing Values)
  - แถวซ้ำ (Duplicates)

**ทำความสะอาดข้อมูล:**

- คลิก "🗑️ ลบค่าว่าง" เพื่อลบแถวที่มีค่าว่าง
- คลิก "🔁 ลบแถวซ้ำ" เพื่อลบแถวซ้ำ

---

### 3️⃣ แท็บ "🤖 เทรนโมเดล"

**ตั้งค่า:**

1. ป้อน "🎯 ชื่อคอลัมน์เป้าหมาย" (เช่น: `price`, `category`)
2. ป้อน "⏱️ จำนวน Epochs" (ค่าเริ่มต้น: 50)
3. ป้อน "📦 Batch Size" (ค่าเริ่มต้น: 32)

**เทรนโมเดล:**

- คลิก "🚀 เริ่มเทรนโมเดล"
- โปรแกรมจะ:
  - เตรียมข้อมูล
  - สร้างโมเดล Neural Network
  - เทรนโมเดล
  - แสดง Accuracy

**บันทึกโมเดล:**

- ป้อน "💾 ชื่อโมเดล" (เช่น: `my_meat_model`)
- คลิก "💾 บันทึกโมเดล"
- ไฟล์จะบันทึกใน `models/`:
  - `my_meat_model.h5` (Keras format)
  - `my_meat_model.tflite` (TensorFlow Lite format)

---

## 📊 ตัวอย่างข้อมูล (CSV)

```csv
feature1,feature2,feature3,price
10.5,5.2,3.1,100
12.3,6.1,4.2,150
9.8,4.9,2.8,95
```

---

## 🔧 หากเกิดข้อผิดพลาด

### TensorFlow ไม่ติดตั้ง

```bash
pip install tensorflow
```

### CustomTkinter ไม่ติดตั้ง

```bash
pip install customtkinter
```

### Pandas ไม่ติดตั้ง

```bash
pip install pandas openpyxl
```

### ลบข้อมูลใหม่

```bash
rmdir data /s
rmdir models /s
```

---

## 💡 เคล็ดลับ

- **ก่อนเทรน:** ตรวจสอบและทำความสะอาดข้อมูลเสมอ
- **Epochs มาก:** เทรนช้าแต่ผลลัพธ์อาจดีขึ้น
- **Batch Size น้อย:** ใช้ RAM มาก แต่เทรนช้า
- **TFLite:** ใช้สำหรับการ Deploy บน Mobile

---

## 📝 ข้อมูลเพิ่มเติม

- **CustomTkinter:** UI Framework สำหรับ GUI
- **TensorFlow/Keras:** Deep Learning Library
- **Pandas:** Data Processing
- **Scikit-learn:** Machine Learning Utilities

---

## 📄 License

ใช้ได้อย่างอิสระ

---

## 👨‍💻 Developer

สร้างโดย: AI Assistant
